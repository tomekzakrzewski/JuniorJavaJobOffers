#database
spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://localhost:3306/junior-offers
    username: root
    password: tomek
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
#email config
  mail:
    host: smtp.gmail.com
    port: 587
    username: juniorjavajoboffers@gmail.com
    password: pfhdvtnmwkwzizgv
    default-encoding: UTF-8
    properties:
      mail:
        mime:
          charset: UTF
        smtp:
          writetimeout: 10000
          connectiontimeout: 10000
          timeout: 10000
          auth: true
          starttls:
            enable: true
            required: true
    verify:
      host: http://localhost:8080
#http
offer:
  http:
    client:
      config:
        connectionTimeout: 1000
        readTimeout: 1000
        uri: https://nofluffjobs.com/api/search/posting?page=1&salaryCurrency=PLN&salaryPeriod=month&region=pl
